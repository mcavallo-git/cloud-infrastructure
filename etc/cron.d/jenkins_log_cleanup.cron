#
# DAT_CRON="/etc/cron.d/jenkins_log_cleanup.cron" && \
# vi "${DAT_CRON}" && \
# chown "root:root" "${DAT_CRON}" && \
# chmod 0644 "${DAT_CRON}" && 
# touch "$(dirname ${DAT_CRON})" && \
# service cron restart;
#

35 00 * * * root /usr/local/sbin/jenkins_log_cleanup;
