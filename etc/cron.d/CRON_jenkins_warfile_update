#
# DAT_CRON="/etc/cron.d/CRON_jenkins_warfile_update" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}"; /bin/systemctl restart "cron" 2>'/dev/null'; /bin/systemctl restart "crond" 2>'/dev/null';
#

### [ JENKINS ] Update the main Jenkins executable (named a 'warfile', which has a '.war' file extension)
10 3 * * * root test -x "/usr/local/sbin/update_jenkins_warfile" && "/usr/local/sbin/update_jenkins_warfile";
