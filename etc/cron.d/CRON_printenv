#
# DAT_CRON="/etc/cron.d/CRON_printenv" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}" && service cron restart;
#
#
# * * * * * root sudo printenv > /var/log/CRON_printenv;
#