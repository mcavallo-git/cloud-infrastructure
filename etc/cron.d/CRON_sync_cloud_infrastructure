#
# DAT_CRON="/etc/cron.d/CRON_sync_cloud_infrastructure" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}" && service cron restart;
#

### [ SYNC ] Sync cloud infrastructure repo @ https://github.com/mcavallo-git/cloud-infrastructure/blob/master/README.md
* * * * * root wget "https://raw.githubusercontent.com/mcavallo-git/cloud-infrastructure/master/usr/local/sbin/sync_cloud_infrastructure" --output-document="/root/sync_cloud_infrastructure" && chmod 0700 "/root/sync_cloud_infrastructure" && /root/sync_cloud_infrastructure --cron sync && rm "/root/sync_cloud_infrastructure";
