#
# DAT_CRON="/etc/cron.d/CRON_other_devtools" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}" && service cron restart;
#

### [ DEV-TESTING ] Check/verify/etc. a given user's environment variables
# * * * * * root sudo printenv > /var/log/CRON_printenv;
