#
# DAT_CRON="/etc/cron.d/CRON_raspi_hw_monitor" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}"; SERVICE_NAME="cron"; /bin/systemctl restart "${SERVICE_NAME}" 2>'/dev/null'; SERVICE_NAME="crond"; /bin/systemctl restart "${SERVICE_NAME}" 2>'/dev/null';
#

### [ Raspberry Pi ] Monitor & log ongoing max/min temps as time goes on
* * * * * root test -x "/usr/local/bin/raspi_hw_monitor" && "/usr/local/bin/raspi_hw_monitor";
