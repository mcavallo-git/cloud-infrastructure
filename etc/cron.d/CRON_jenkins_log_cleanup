#
# DAT_CRON="/etc/cron.d/CRON_jenkins_log_cleanup" && echo "" > "${DAT_CRON}" && vi "${DAT_CRON}" && chown "root:root" "${DAT_CRON}" && chmod 0644 "${DAT_CRON}" && service cron restart;
#

25 01 * * * root /usr/local/sbin/jenkins_log_cleanup;
